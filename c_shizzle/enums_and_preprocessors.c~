#include <stdio.h>

int main()
{
  enum types {
    ZERO, ONE, TWO, THREE
  } types;

  printf("first :: %d\n", ZERO);

  //  enum dytes {
  //ZERO, ONE, TWO, THREE
  //} dytes;

  //printf("second :: %d", dytes.ZERO);
  
}

